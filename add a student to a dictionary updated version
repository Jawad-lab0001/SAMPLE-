this is updates version when enter type returns dic
student_list = {}
students = int(input("enter the number of students you want to add : "))
for x in range,{students}:
    student_name ={
        input("enter the students name :")
        }
    student_ID ={
        int(input("Enter the student ID :"))
        }
    Course = {
        input("enter the course :")
        }
    student_list [students] = student_name, student_ID,   Course,
    print((type)(student_list))
